/* eslint-disable react/prop-types */
const SectionTitle = ({ title }) => {
    return (
        <h2 className="text-white text-center text-md md:text-3xl font-sans font-bold py-8">
            {title}
        </h2>
    );
};

export default SectionTitle;